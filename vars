export JEKYLL_VERSION=4.0

export ARGS=(--rm
    --interactive
    --tty
    --mount type=bind,source="${PWD}",target='/srv/jekyll'
    --mount type=bind,source="${PWD}/vendor/bundle",target='/usr/local/bundle'
    --mount type=bind,source="$(readlink -f ${PWD}/confidential)",target='/srv/jekyll/confidential' \
    jekyll/jekyll:"${JEKYLL_VERSION}")
